import 'package:flutter/material.dart';

class NegociacaoColuna extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Negociações:',style: TextStyle(fontSize: 25),),
        
      ],
    );
  }
}
